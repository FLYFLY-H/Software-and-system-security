#include<stdio.h>
#include<Windows.h>
#define circle_time 5
int  ec=6;
int ed =8;
int random(int a,int b);
int main()
{
	int a =0;
	int b=1;
	int c[] = {1,-1,3,-2,3,-4,2,1};
	int time = circle_time;
	for(int i =0;i<8;i++)
	{
		if(c[i]>0)
			c[i]=1;
		else c[i]=0;
                 ed--;
	}
	do
	{
		time--;
                 ec++;
		a=random(a,c[time]);
		switch (a)
		{
		case 0:
			a++;
		case 1:
			a--;
		case 2:
			a++;
		case 3:
			a--;
		case 4:
			a++;
		case 5:
			a--;
		case 6:
			a+=a;
		case 7:
			a=0;
		default:
			a=0;
		}
		
	}while(time);
	MessageBox(NULL,TEXT("dede"),TEXT("dede2"),MB_OK);
	return 0;
}
int dede(int a)
{
   ec++;
   return a;

}
int random(int a,int b)
{
	 b =dede(a);
	a=(a+b*35)%10+2;
	ec++;
        ed--;
	if( a<3) return a;
	else return b+3;
}
